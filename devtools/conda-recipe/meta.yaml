package:
  name: cmiles
  version: "0.1.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python
    - rdkit
    - openeye-toolkits

test:
  requires:
    - pytest
  imports:
    - cmiles

about:
  home: https://github.com/openforcefield/cmiles
  license: MIT License
